{% include 'common/templates/header.j2' %}

filter f_restic {
    program("ansible-restic-*" type(glob));
};

destination d_restic {
    file("/var/log/restic/${PROGRAM}.log" create_dirs(yes));
};

log {
    source(s_src);
    filter(f_restic);
    destination(d_restic);
};
